{
  "name": "bibtex-parser",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cd dist && git init && git add -A && git commit -m 'deploy' --allow-empty && git push -f https://github.com/dynaroars/bibtex.git main:gh-pages"
  },
  "devDependencies": {
    "vite": "^5.4.2"
  },
  "dependencies": {
    "bibtex-parse": "^2.1.0"
  }
}